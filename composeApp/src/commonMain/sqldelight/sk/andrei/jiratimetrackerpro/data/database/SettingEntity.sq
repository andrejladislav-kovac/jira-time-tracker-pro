CREATE TABLE SettingEntity (
     id TEXT PRIMARY KEY NOT NULL,
     value TEXT
);

findById:
SELECT * FROM SettingEntity WHERE id = ? LIMIT 1;

findByIds:
SELECT * FROM SettingEntity WHERE id IN ?;

upsert:
INSERT OR REPLACE INTO SettingEntity (id, value)
VALUES (?, ?);